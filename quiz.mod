A.¿Cuál es la ventaja de que JavaScript sea un lenguaje 
interpretado-compilado?
 -- Que se ejecuta en tiempo real, * *just in time compilation* *

B.¿Cuál es la diferencia entre null y undefined?
 -- Entiendo por null que es un valor que es vacio y
 undefinided es que es un algo que no existe. 

C.Explica el concepto de ​type coercion
  --Yo entendí  que es el proceso que usa Js de convertir un valor 
  de un tipo a otro, por ejemplo de numero a cadena de texto, de 
  manera que lo convierta en un objeto válido.
  Para hacer una coercion simple usamos == y para identificar  que sea
  un elemento del mismo tipo usamos === 

 true && !(false === !infinity); //

    true && !( false === (!true)); 
    true && !( false === (false)); 
    true && !(True); 
     True && False; 
   ** Answer: False**


 null !== undefined && !"": // 
  null !== undefined && !false: //
   null !== false && true // 
    null !== false // 
    null == True
    Answer: False

